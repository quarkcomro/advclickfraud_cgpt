# User manual

The module starts disabled and observe-only by default.

Configure trusted proxy IP addresses before enabling JA4 header correlation.

Run functional tests in a PrestaShop 8.2 or 9.x instance before production use.
